<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="16dp"
    android:paddingRight="16dp">

    <TextView
        android:id="@+id/domain_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_toLeftOf="@+id/change_domain_list_button"
        android:layout_centerVertical="true"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:textSize="17sp"
        android:paddingTop="10dp"
        android:paddingBottom="10dp"/>
    <Button
        android:id="@+id/change_domain_list_button"
        android:layout_width="110dp"
        android:layout_height="40dp"
        android:layout_centerVertical="true"
        android:textSize="8sp"
        android:layout_toLeftOf="@+id/domain_info_button"
        android:paddingEnd="16dp"
        android:paddingStart="16dp"
        />

    <ImageButton
        android:id="@+id/domain_info_button"
        android:src="@drawable/ic_drop_down"
        android:background="@null"
        android:layout_width="15pt"
        android:layout_height="15pt"
        android:layout_centerVertical="true"
        android:layout_alignParentRight="true"
        android:paddingEnd="16dp"
        android:paddingStart="16dp"/>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@android:color/darker_gray"
        android:layout_below="@id/domain_name"/>

</RelativeLayout>